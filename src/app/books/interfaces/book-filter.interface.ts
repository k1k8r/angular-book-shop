import { IAuthor } from '@app/authors';
import { IGenre } from '@app/genres';

export interface IFilterDialog {
  // author: IAuthor[];
  genre: string;
  // writingDate: Date;
  // maxPrice: number;
  // minPrice: number;
}
